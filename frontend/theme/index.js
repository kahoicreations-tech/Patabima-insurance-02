// Central export for PataBima theme tokens
import colors, { BRAND, SEMANTIC, UI, STATUS } from './colors';
import typography, { FONT_SIZES, FONT_WEIGHTS, LINE_HEIGHTS, LETTER_SPACING, FONT_FAMILIES, TEXT_PRESETS } from './typography';
import spacing, { SPACING, BORDER_RADIUS, BORDER_WIDTH, SAFE_AREA } from './spacing';
import shadows, { SHADOWS } from './shadows';

export { colors, typography, spacing, shadows };
export { BRAND, SEMANTIC, UI, STATUS } from './colors';
export { FONT_SIZES, FONT_WEIGHTS, LINE_HEIGHTS, LETTER_SPACING, FONT_FAMILIES, TEXT_PRESETS } from './typography';
export { SPACING, BORDER_RADIUS, BORDER_WIDTH, SAFE_AREA } from './spacing';
export { SHADOWS } from './shadows';

export default { colors, typography, spacing, shadows };
