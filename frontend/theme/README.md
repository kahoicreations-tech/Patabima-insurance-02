# PataBima Design System (Theme)

Quick usage:

- Tokens: import from `frontend/theme`
- Components: use shared components in `frontend/components/common`

Examples:

import { BRAND, SPACING, FONT_SIZES } from '../theme';

// Styles
// const styles = StyleSheet.create({
// container: { padding: SPACING.md, backgroundColor: BRAND.primary },
// title: { fontSize: FONT_SIZES.h1 },
// });

See .github/prompts/11-design-system-implementation.prompt.md for full guidance.
